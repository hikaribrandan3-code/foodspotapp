# 2026-03-23 - Claude Code Installation

- **Task:** User asked about installing Claude Code and which terminal to use.
- **Status:** Already installed on the system (v2.1.81).
- **Advice Given:**
  - Recommended installing in the main terminal (macOS/Terminal.app/iTerm2).
  - Since Antigravity runs on the same host, it's shared.
  - Provided `curl` and `npm` installation commands for reference.
  - **Magic Word:** `claude` is the command to activate it in the terminal.
  - **Setup:** Mentioned they might need to run `claude auth` for first-time login.

## Notes
- User is Hikari, based in Argentina.
- Working on GrubClubApp (FoodSpot-OS).
- Currently active in `pool/index.html`.
