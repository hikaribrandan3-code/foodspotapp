# 2026-04-23 Session Log

## Cash Order Pipeline Audit (COMPLETED)
- **Problem:** Cash orders went to Summary but vanished from Orders/Analytics/Staff tabs
- **Root Cause:** 4 separate code paths corrupted data differently
  1. `payment_method` overwritten to `'cash'` in `offlinePayment.js` — broke cash filters
  2. `DeliveryManager.jsx` used Spanish statuses + camelCase + no `business_id` filter
  3. Two competing status languages (English vs Spanish)
  4. `syncOfflinePayment` race condition — cash orders stayed at `'pending'`, invisible to staff
- **Fixes Applied (6 files):**
  - `Order.jsx` — Cash → `paid_unreleased`, MP → `pending_payment`
  - `orderStateGuard.js` — Full rewrite: Spanish → English Strike 16 FSM
  - `supabaseClient.js` — Default status `pending_payment`
  - `storage.js` — `'entregado'` → `'delivered'`
  - `DeliveryManager.jsx` — Spanish→English, camelCase→snake_case, +business_id silo filter
  - `SuperAdmin.jsx` — All Spanish statuses → English
- **Commits:** `4741838`, `69f0f10`, `722d4d0`

## Strike 16 FSM Standardization
- Database CHECK constraint enforces English enum: `pending_payment`, `paid_unreleased`, `released_to_kitchen`, `preparing`, `ready`, `dispatched`, `delivered`, `refunded`, `cancelled`
- Previous code inserted illegal values (`'pending'`, `'pendiente'`) which would fail CHECK constraint
- All frontend now aligned with Strike 16 migration

## Staff Dashboard Critical Error (UNRESOLVED)
- **Error:** `TypeError: null is not an object (evaluating 'f[x]')`
- **Location:** Minified Supabase chunk `supabaseClient-Jw-lH6wk.js`
- **Scope:** `/staff/dashboard` route only, Vercel production only
- **Status:** Pre-existing bug (present before my changes)
- **Claude Handoff:** Provided full audit summary with suspected causes (Realtime disabled, Vite tree-shaking, version mismatch, race condition)
- **Action:** Hikari will pass to Claude for debugging

## KDS Status Conflict (FLAGGED)
- `useKDSSync.js` / `StaffKDS.jsx` uses statuses: `paid`, `cooking`, `ready`, `completed`
- These VIOLATE Strike 16 CHECK constraint (`paid`, `cooking`, `completed` not in enum)
- KDS is either broken silently or migration never ran
- **Fix needed:** Map KDS → Strike 16: `paid`→`released_to_kitchen`, `cooking`→`preparing`, `completed`→`delivered`

## Credentials Secured
- All tokens stored in `SECRETS.md` (not committed)
- Includes: GitHub, Vercel, Supabase, Groq, Kimi Code, Mercado Pago (prod + sandbox), Gmail, Apple iCloud
- **Rotation recommended:** Supabase Service Role Key, Mercado Pago Access Tokens

## Code Red Mode
- Active: March 29 – April 10, 2026 (Launch Window)
- Current status: Hikari working on KDS after cash pipeline fixes

## Notes
- Hikari uses OpenCode (Build/Big Pickle modes), ctrl+p for commands, tab for agents
- Working directory: `~/Desktop/GrubClubApp:main`
- Factory Belt Git model: Hikari works in sandbox, pushes PRs, Tech Beast merges
- Terminal bridge: OpenCode (preferred over Aider), Claude Code (v2.1.81 active)
- Styling: Vanilla CSS only, CSS variables for branding
- Payment: Mercado Pago (Sandbox active), test transaction: Ghost Burger $10.00 PAID

## Webhook Debugger
- Clawhub `webhook-debugger` flagged as suspicious by VirusTotal Code Insight
- Not installed — risk of crypto keys/external APIs/eval in code
- Alternative: Build custom webhook logger when needed
