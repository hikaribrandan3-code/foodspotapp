## Session: April 24, 2026

### FoodSpot-OS / GrubClubApp Work

**Fixed: Category Icons in OrderStatusEmpty.jsx**
- `getCategoryEmoji()` now accepts `(categoryName, index)` and cycles drink categories through 🥤🧃🥛🍹🧉☕🍵 to guarantee unique icons per slot
- Broke out alcohol (🍷) from beer (🍺) for more variety
- Added 8+ more food mappings: tarta🥧, empanada🥟, hot dog🌭, soup🍲, fish🐟
- Card description deduped: shows categoryName when `description === name` (no more "Extreme burger / Extreme burger")
- Flipped category priority: `menu_data` (correct user config) → DB fallback. Fixes duplicate drink categories showing from stale DB table.

**Fixed: Build Error**
- Accidentally deleted `useEffect(() => {` opening when inserting fallback code. ESBuild error: Expected ';' but found ')' at line 179. Restored.

**Reverted: Home.jsx changes**
- Mistakenly added category rail + 4-square grid to Home.jsx when user wanted it on OrderStatusEmpty.jsx. Reverted Home.jsx back to original.

**Reverted: Arcade Card Marquee**
- User said scrolling emoji marquee looked "wack". Reverted to simple 🎮 Play a game! card.

**Fixed: Owner Orders Crash (React Error #31)**
- `order.delivery_address` is stored as an **object** `{floor, notes, number, street}` in the database.
- Rendering it directly as a React child caused React error #31 (Objects are not valid as React children).
- Added `formatAddress()` helper in DeliveryManager.jsx that converts the object to a human-readable string.

**Style: Home Padding**
- Reduced Home.jsx horizontal padding from `12px` to `8px` to match Menu page tightness. Less white space on iPhone 17 Pro Max.

**Style: Hero Cover Height**
- After multiple iterations (242px → 160px → 180px), locked final hero height at **180px** on mobile.
- Applied same cap to CoverImageEditor.jsx so preview matches app render.
- Result: Logo visible with sparkle, 62px more screen space for content, consistent across all phones.

**User Preferences Noted**
- Likes unique category icons, not repeated emojis
- Prefers simple, clean UI over animated gimmicks (rejected scrolling marquee)
- Menu and Info tab padding/margin style is preferred over Home's previous wider padding
- Focus is on "main stuff" — not cosmetic flourishes right now
- **CRITICAL: I only code when Hikari explicitly says so. Planning mode is default.**
- Uses KimiCode (terminal) for actual coding, I provide prompts/audits

**Architecture Notes**
- `tenantData.menu_data.categories` is the source of truth for categories (set by user in menu builder)
- Old `categories` DB table has stale/duplicate entries and should be treated as fallback only
- `order.delivery_address` in Supabase is an object, not a string — any renderer must handle this
- `customerInfo.address` is ALSO an object `{street, number, floor, notes}` — same crash risk

**Audit Findings (vs Claude)**
- Found 2 more React #31 crashes Claude missed: `SuperAdmin.jsx:2049` and `OrdersTab.jsx:85`
- Found stale ghost grid in CoverImageEditor.jsx still showing deleted featured photos system
- Found copy mismatch: editor says "center food" but users upload logos
- Prompt provided to KimiCode for fixing all 5 audit items

**Forgot Password Flow — Pending**
- User wants "Forgot?" link on signin to send reset code to email
- Currently shows `alert('Password reset coming soon')`
- No existing reset flow in codebase
- Using Supabase Auth — has built-in password reset via email
- Prompt to be sent to Claude Code
