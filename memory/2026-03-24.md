

---

# March 24, 2026 - Evening Session: Analytics, AI & Camera Audit

## Files Audited Today (Continued)

### 1. Analytics.jsx - ✅ PRODUCTION READY
- Real Supabase data fetching with proper tenant isolation
- Date range filtering (today/week/month/all)
- KPI grid: revenue, order count, avg ticket, delivered count
- Order type breakdown, payment method split, top products
- Clean implementation, no issues found

### 2. FoodSpotAI.jsx - 🔧 NEEDS "COMING SOON" MODE
**Current State:** Image generation is NOT hidden from users
- Text chat (Gemini 2.0 Flash) - ✅ LIVE
- Image generation (Pollinations AI) - ⚠️ ACCESSIBLE

**Required Fixes:**
1. Add `ENABLE_IMAGE_GENERATION = false` flag
2. Rename UI to "AI Assistant ✨ — Text & Strategy Mode"
3. Comment out image generation path
4. Post-launch: Move API key to Edge Function proxy (currently exposed in URL)

### 3. Camera Engine - 🟢 PRODUCTION READY
**Files:** `useCamTech.js`, `useCamera.js`, `CameraLayer.jsx`

**Strengths:**
- CamTech Black Box pauses background sync when camera active
- Hardware lock with 720p fallback
- 10 filter styles (CSS + pixel manipulation)
- Flash/zoom/pinch controls
- Black screen auto-recovery (3s timeout)
- Tab visibility handling (auto stop/start)

**Minor Debt:**
- No tenant watermark on capture (images for internal use)
- Blob URL leak risk (objectURL created but not revoked)
- No permission-denied friendly UI

## Audit Complete - Launch Status

### 🔴 P0 - Must Fix Before Launch:
1. Run `critical_schema_fix.sql` (orders constraint, MP columns, indexes)
2. Fix Settings.jsx `Object.assign` mutation (color revert bug)
3. Fix MenuManager blob URL guard (system error)
4. Add `ENABLE_IMAGE_GENERATION = false` to FoodSpotAI.jsx
5. Fix useSplitPayment.js transaction safety (race condition)

### 🟡 P1 - Should Fix Before Launch:
1. Migrate DeliveryManager from localStorage to Supabase realtime
2. Verify email verification is enabled in Supabase Auth
3. Fix Info.jsx dead Rappi/PedidosYa buttons
4. Add role guard to Info.jsx Admin Access

### 🟢 P2 - Post-Launch Polish:
1. API key proxy through Edge Function
2. Camera blob URL cleanup
3. SuperAdmin DEV bypass button hardening
4. Staff PIN hashing improvement

## Silent Beta Launch: Week 2 April
3-5 friendly vendors, weekly updates expected, no marketing spend.

---

# WORKFLOW PROTOCOL — ESTABLISHED MARCH 24, 2026

## Agent Stack & Responsibilities

| Agent | Role | When to Use |
|-------|------|-------------|
| **Tech Beast (Gemini Custom)** | Implementation / Code execution | Primary executor for fixes from audit |
| **Kimi (me)** | Analysis / Small fixes / Review | Tasks with <3% error margin |
| **Claude Opus 4.6** (Claude Code OR Antigravity) | Complex bundled tasks | When bundling multiple fixes together |
| **Notebook LLM** | Marketing / Pitch / Narrative | YC application, competitive analysis, story gaps |

## Decision Tree

```
Tech Beast suggests fix
        ↓
    [Pass to Kimi]
        ↓
    Can I handle with 
    <3% error margin?
        ↓
    YES → I execute
    NO  → Bundle with other 
          complex tasks → 
          [Send to Opus 4.6]
```

## Rules
1. **Always analyze first** — I evaluate complexity before committing
2. **Bundle for Opus** — Don't send puzzles; send complete instruction packages
3. **Notebook LLM gets narrative** — Never feed it raw technical audits
4. **Weekend = Marketing** — Sat/Sun is Notebook LLM time for YC/pitch work

## Available Streams
- Claude Code ( Anthropic )
- Antigravity ( Anthropic — Opus 4.6 )
- Both can run Opus when needed
